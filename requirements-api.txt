# Lean runtime dependencies for the prediction API container.
# The full requirements.txt (notebooks, MLflow, Streamlit, plotting) is not
# needed to serve predictions and roughly triples image size / boot time.
#
# pandas is pinned below 3.0: the model artifacts were fitted under pandas 2.x,
# and the preprocessing code is now dtype-robust across 2/3, but pinning keeps
# inference numerically reproducible with how the model was trained.
pandas>=2.0.0,<3.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
xgboost>=2.0.0
shap>=0.43.0
matplotlib>=3.7.0  # imported at module load by models/explainer.py
joblib>=1.3.0
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
python-dotenv>=1.0.0
supabase>=2.0.0

# Multi-tenant SaaS layer (auth, customer storage, outreach)
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0   # Postgres driver so DATABASE_URL can point at Supabase
pyjwt>=2.8.0
python-multipart>=0.0.9   # CSV upload endpoints
email-validator>=2.0.0    # pydantic EmailStr
httpx>=0.24.0             # Resend outreach delivery
